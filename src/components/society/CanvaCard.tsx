// CanvaCard was removed per user request. Intentionally keep a no-op stub to avoid
// accidental usage; the user will add their own card using the existing society
// creation flows. Export a harmless component.

export default function CanvaCard() {
  return null;
}
